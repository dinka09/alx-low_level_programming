#include "main.h"
/**
 * _islower - checking if the character is lowercase
 * @c: a character to bec heched
 * Return: retruns 1 nad 0 based on the given character
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
