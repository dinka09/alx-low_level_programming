#include "main.h"
/**
 * _abs - computing absolute value of the integer
 *@n: Absolute valu og the given number
 * Return: Absolute value of the number
 */
int _abs(int n)
{
	if (n < 0)
	n = -n;
	return (n);
}
