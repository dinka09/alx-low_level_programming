#include "main.h"
/**
 * mul - functions that multiply two numbers
 * @a: first number
 * @b: second number
 * Return: success integer
 */
int mul(int a, int b)
{
	int mul;

	mul = a * b;
	return (mul);
}
