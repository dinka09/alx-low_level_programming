#include <stdio.h>
/**
 * main - printing one digit base 10 numbers
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int x;

	for (x = 0; x <= 9; x++)
	{
		putchar(x);
	}
	putchar('\n');
	return (0);
}
