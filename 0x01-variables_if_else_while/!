#include <stdio.h>
/**
 * main - Printing numbers and alphabets
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int x;
	char alphabet = 'a';

	for (x = '0'; x <= '9'; x++)
	{
		putchar(x)
	}
	for (alphabet <= 'f'; alphabet++)
	{
		putchar(alphabet)
	}
	putchar('\n');
	return (0);
}
